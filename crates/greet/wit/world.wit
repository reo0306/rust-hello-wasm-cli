package reo0306:helloworld;;

interface greet {
    export hello-world: func() -> string;
    export greet: func(name: string) -> string;
}

/// An example world for the component to target.
world example {
    export greet;
}
